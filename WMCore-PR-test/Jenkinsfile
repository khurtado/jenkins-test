pipeline {
    agent {
        label 'cms-dmwm-el9-01'
    }

    environment {
        MY_HOSTNAME = """${sh(
            returnStdout: true,
            script: 'hostname -f'
        ).trim()}"""

        MY_ID = """${sh(
            returnStdout: true,
            script: 'id -un'
        ).trim()}"""

        MY_GROUP = """${sh(
            returnStdout: true,
            script: 'id -gn'
        ).trim()}"""

        HOST_MOUNT_DIR = "${env.WORKSPACE}"
        
        WMA_TAG = '2.3.3'
        COUCH_TAG = '3.2.2'
        MDB_TAG = '10.6.5'

    }

    stages {
        stage('Test compose') {
            steps {
                git branch: 'main', url: 'https://github.com/d-ylee/jenkins-test'
                sh '$WORKSPACE/WMCore-PR-test/setup-users.sh'
                sh 'cat $HOST_MOUNT_DIR/admin/etc/passwd'
                sh 'cat $HOST_MOUNT_DIR/admin/etc/group'
                sh 'docker compose -f $WORKSPACE/WMCore-PR-test/docker-compose.yml up -d'
            }
        }
    }

    post {
        always {
            sh 'docker compose -f $WORKSPACE/WMCore-PR-test/docker-compose.yml down'
        }
    }
}
